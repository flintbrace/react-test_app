const Header = ({ title }) => {
  return (
    <header>
      <h1>{title}</h1>
      <p>Here's what We'll be Modifying</p>
      <p>Here's our First Modification</p>
    </header>
  );
};

export default Header;
